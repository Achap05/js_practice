// Créez une page Web qui demande un nom et l’affiche

'use strict'
  let userName = prompt("Nom");
  alert( userName );

  // le script sort un champ de saisie "nom" et affiche ensuite la valeur entrée